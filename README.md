# Installation
```bash
cd WiDAQFlows # meaning the root folder of the repo
npm i
cd settings
npm i
```
or if you perfer yarn
```bash
cd WiDAQFlows # meaning the root folder of the repo
yarn
cd settings
yarn
```
